# Work Log — 11 September 2026

**6 commits pushed to `main`** (4 work batches).

## 1. Admin Dashboard: Real metrics + NGN currency (`255f5a1`)

**Found:** Conversion Funnel chart and Acquisition Sources pie were hardcoded/fake; Monthly Revenue card always equaled Total Revenue; currency hardcoded as `$`.

**Fixed:**
- Backend: `admin.routes.ts` — `monthlyRevenue` now sums `revenueTrend.rows` for the selected range.
- Frontend `Dashboard.tsx`: funnel renders real `conversionFunnel.funnel` (3-stage + rate cards), pie renders real `sourceAnalytics` (top 5 + empty states), revenue displays use `formatCurrency` (NGN).

## 2. About Page Team updates (`c9b1bd6`, `c558e76`, `c9823a6`)

- CEO: **Dr. Alex Rivera → Dev Chris** + real photo (`ceo-dev-chris.png`).
- Head of Curriculum: **David Kim → Success Chukwu** + real photo (`success.jpg`).
- CTO: **Maya Patel → Ike Wisdom** + real photo (`wisdom.jpg`).
- All `Press.tsx` author references updated too. `/success.jpg` was never committed initially → fixed as a separate asset commit (Vercel could not serve it until then); also renamed from its original filename.

## 3. Learning Paths: public detail page + empty-path sync (`a5b7c28`)

**Found:** `/learning-paths/:slug` returned 404 (no route existed); GuidedMode linked to that broken public URL; `software-development-beginner` existed but had **0 linked courses**.

**Fixed:**
- Added public route `/learning-paths/:slug` reusing `LearningPathDetail` with a `variant` prop (back-link: public → `/courses`, student → `/student/learning-paths`).
- `getGroupedBySchool()` now rebuilds courses for an existing school path that has zero links but matching published courses.
- GuidedMode now links to the student route.
- **DB backfill:** 24 school paths, 39 course links (idempotent).

## 4. Lesson Quizzes: server-graded with review (`33e121f`)

**Found:** embedded `LessonQuiz` scored client-side, but `correct_answer` is stripped for students → always showed 0%. No server submission → attempts never recorded → the progress `quizRequired` gate could never be satisfied. `QuizTake` review was equally broken for students.

**Fixed:**
- Backend `quiz.routes.ts`: `POST /quizzes/:id/submit` now returns `questionResults` (per-question `correctAnswer`, `userAnswer`, `isCorrect`) plus `attemptsUsed`/`maxAttempts`/`passingScore`.
- `LessonQuiz.tsx` rewritten as a proper inline quiz player: submits to server (records attempt, awards XP, deducts hearts), timer, progress, one-question navigation, results + per-question review, retry while attempts remain, prior-pass banner.

## Pending

- **Render manual redeploy** needed for backend: monthly-revenue calculation (batch 1) and quiz `questionResults` response (batch 4).
- Frontend is live via Vercel auto-deploy.